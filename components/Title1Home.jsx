import React from 'react';

const Title1Home = ({ title }) => {
  return (
    <div className="text-center uppercase text-[16px] md:text-xl mb-[40px] roboto_condesed text-white">
      {title}
    </div>
  );
};

export default Title1Home;
