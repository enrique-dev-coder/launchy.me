import React from 'react';

const Title1Home = ({ title }) => {
  return (
    <div className="text-center uppercase text-xl mb-[40px] roboto_condesed text-white">
      {title}
    </div>
  );
};

export default Title1Home;
